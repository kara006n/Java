package abstraction2;
//here abstract class implements interface but there is no error because,,,we generally get error when implement interface in class because now 
//class gets incomplete methods and we know its not allowed in class but in abstarct class we can have incomplete methods so no error

//incomplete test() method is inherited
//test2() of this class
public abstract class B implements C {
		public abstract void test2();	
}
